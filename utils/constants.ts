/**
 * Global fragment caps for performance. The mobile limit is used when we
 * detect smaller viewports via a simple width threshold.
 */
export const FRAGMENTS_LIMIT = 220;
export const MOBILE_FRAGMENTS_LIMIT = 110;
